# --- HarmonyOS build outputs ---
# Note: /build/ contains committed source (build scripts, vite config).
# Only ignore HarmonyOS build outputs and electerm-web build artifacts.
/build/outputs/
/build/intermediates/
/oh_modules/
/entry/build/
/web_engine/build/
/.hvigor/
/.cache/
/.cxx/

# --- Signing materials (NEVER commit) ---
/signing/*
!/signing/.gitkeep
*.p12
*.cer
*.p7b
*.csr

# --- Electron runtime (downloaded by prepare-electron-runtime.sh, not committed) ---
# web_engine/ is the entire HAR module from the prebuilt tarball
/web_engine/
# .so libraries extracted from the tarball
/entry/libs/

# --- Generated build-profile.json5 (generated by build-app.sh) ---
/build-profile.json5

# --- electerm build output ---
/work/
/build/harmony/resfile/
/.env
/dist/

# --- Node.js ---
node_modules/

# --- IDE ---
.idea/
.vscode/
*.iml

# --- OS ---
.DS_Store
Thumbs.db

# --- Temp ---
/temp/
# Build download cache
/build-download/
build/harmony/rawfile
src/client/electerm-react/
entry/src/main/resources/rawfile
/src/client/electerm-react/
/data
.workbuddy
# --- Web (ArkWeb) build outputs ---
# electerm-web app bundled into the entry module resfile by build/web/build.mjs
/entry/src/main/resources/resfile/
# node runtime download cache
/.cache/
# hvigor native (CMake/Ninja) build dir & ohpm installs
entry/.cxx/
entry/oh_modules/
# generated local SDK paths (written by scripts/build-web-app.sh)
local.properties
